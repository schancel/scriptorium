{
  "name": "scriptorium",
  "private": true,
  "type": "module",
  "description": "Learn to type by copying the Bible.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "tsc && node --test \"build/core/**/*.test.js\"",
    "smoke": "tsc && node tools/smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
