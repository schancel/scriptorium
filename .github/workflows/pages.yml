name: deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: npm install --no-audit --no-fund
      - name: Build
        run: make build
      # Stage only what the site needs. Uploading '.' would ship node_modules
      # and the whole git-tracked repo, which is slow and hits artifact limits.
      - name: Stage site
        run: |
          mkdir -p _site
          cp index.html _site/ 2>/dev/null || { echo "no index.html yet -- nothing to deploy"; exit 1; }
          cp -r build _site/
          cp -r data _site/
          touch _site/.nojekyll
          du -sh _site
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with: { path: '_site' }
      - id: deployment
        uses: actions/deploy-pages@v4
